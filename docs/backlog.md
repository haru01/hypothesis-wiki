# 改善バックログ

このリポジトリ（スキーマ層・ツール・スキル）の改善余地のうち、**いま実害が出ているもの**を載せる。
実害が無いものは「保留」に一行で退避し、見送った判断は「方針確定・不採用」に理由だけ残す。

対応済み項目は本文に残さない。経緯は git 履歴で辿る（[README.md](README.md) の「消えた記録の辿り方」）。

ID 接頭辞は改善の軸を表す: **OI**＝オントロジー ／ **SI**＝スキル ／ **KG**＝ナレッジグラフ ／
**AR**＝アーキテクチャ。番号は軸ごとに既存最大+1で採番し、**再利用しない**
（過去の番号は欠番のまま。学びレコードやコード docstring からの既存参照が意味を保つため）。

---

## P1. 実害あり（いま直す）

（現在なし）

---

## 保留（実害が出たら着手）

いずれも「困っていないので着手しない」もの。**契機を満たしたら P1 に上げる**。

| ID | 課題 | 着手の契機 |
|---|---|---|
| KG-07 | セグメント・インタビュイー・競合がノードでなく、散文の表層形が野放し（同じ対象が複数の呼称で書かれる）。推奨は 案2＝`<PREFIX>-SEG-NNN` のノート化 → 案3＝alias 表＋lint の順 | 案件が増え、名寄せの揺れが実害になったとき。OI-E 群と同じ設計会議で扱う |
| OI-E1〜E4, G2 | オントロジーの表現力拡張: 仮説クラスタ／TEST→H の支持・反証の極性／DEC→H 直接参照／DEC→DEC の supersedes・対抗仮説・Supersession | オントロジーの設計合意を取るとき（KG-07 と同じ土俵） |
| KG-08 | 評価ハーネス（gold set / F1）が無く、スキル改訂の効果を測れない | 実案件で `/learning` の出力揺れが問題になってから（`/lint` の無作為サンプル点検の記録が材料になる） |
| SI-017 | アクティブプロジェクトの切り替えが `.env` の `CURRENT_PROJECT` 手編集頼みで、専用スキルが無い | 案件が3つ以上になったとき |
| SI-020 | `/building` が純CPF段階（ソリューション仮説がまだ無い）を想定していない。CPF の「ソリューションを見せない」鉄則との緊張が未整理 | CPF で再びプロトタイプが要るとき |
| SI-018 | `/formulating` に重複仮説ガードが無い（`/desk-research` にはある） | 重複起票を実際に踏んだとき |
| SI-021 | `/chabudai` の非対話・自動適用パスが死条文（引き下げは本質的に解釈を伴うため機械的確定にまず当てはまらない） | 誰かがバッチで下げてよいと誤解したとき |
| SI-028 | 同名案件を丸ごと作り直すときの ID 採番方針が未定義（001 から振り直してよいか／別 slug 推奨か） | 案件を作り直すとき |
| SI-029 | `/new-project` の `cp` が環境依存の隠しファイルを巻き込む余地（コピー直後の確認手順が無い） | 想定外のファイルが混入したとき |
| AR-07 残 | `tools/hooks/stop_view_gen.py` の専用テストが未追加 | ビュー生成フックを触るとき |
| SI-004 残 | ステータス語に「取り下げ」を足すかが未決（現在は本文注記で運用） | 取り下げが頻発したとき |
| SI-025 残 | `/lean-canvas` のキャンバス HTML 出力を規約にするかが未決（現在は SVG をインライン埋め込み） | — |

---

## 方針確定・不採用（再検討の契機つき）

「検討済みで見送った／こう決めた」を残す節。**知らずに直しにかかるのを防ぐのが目的**なので、
理由は残し、詳細な論拠は git 履歴に委ねる。

- **レガシーな `evidence-tag` warning は retro-fix しない**（旧 KG-10）— 証拠種別タグは確信度履歴の
  過去行に書くものだが、履歴は追記専用（不変ルール2）なので過去行にタグを足すこと自体が規約違反。
  **構造的に解消不能**。したがって **warning 0 を目指さない。error 0 を不変条件とし、warning は
  真の未達を映す計器として読む**。解消したいなら新しい検証を回して階梯タグつきの行を追記するか、
  `/chabudai` で確信度を引き下げる。
- **`sources/` に残る、移行で削除された回顧型 ACT 宛の wikilink は修復しない** — 不変層を
  書き換えないこと（不変ルール3）を優先した決定。実害は Obsidian 上のリンク切れのみで、
  `/lint` もサイトビルドも対象にしていない。
- **ベクトル検索／ハイブリッド検索・確信度付き BFS 探索ツール・デスクトップGUI・
  コミュニティ検出（Louvain 等）は採らない**（旧 OI-H）— 1案件が十数〜数十レコードの規模では、
  Claude が `wiki/` を直接読む現方式より精度・保守性ともに劣化する。導入コスト過大。
  可視化は Obsidian ＋ `relations` ビューのグラフ診断で足りる。
- **抽出量キャップ（Extraction cap）は該当なし**（旧 KG-09）— 取り込みが人手ペースで、
  暴走コストの経路が無い。`sources/` へのバッチ投入経路を作るなら再検討する。
- **架空判定の推論経路（出典冒頭の宣言・本文マーカー語）は残す**（旧 AR-12 の残り）— 判定の正本は
  TEST/LEARN の `data: real | simulated` に移した。推論経路は `data` 未宣言のレコード（他 vault・
  旧レコード）のための後方互換で、消すと宣言漏れが黙って「実データ」になる。副作用として
  **`SELF-LEARN-005` の `data-provenance` warning は消えない**: 出典（揺さぶり監査メモ）の冒頭が
  *監査対象の*架空性に言及しており、`sources/` は不変層なので書き換えられない。宣言が正しく
  上書きしている状態を可視化している警告なので、**直そうとしなくてよい**。
- **`--no-verify` によるフック迂回は許容する** — 技術的に塞げないし、フックは強制装置ではなく
  規律の道具として置いている。
