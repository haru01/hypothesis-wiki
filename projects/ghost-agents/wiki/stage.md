# 現在ステージ

> ⚠️ **本案件は攻殻機動隊（電脳・義体）の世界設定を借りた、スキル試走用の架空案件。**
> 実顧客・実観測は存在しない。ここに積まれる確信度は**上限8**（`CLAUDE.md`「確信度とステータス」の
> 架空/シミュレーションデータ規定）で、9-10 には決して上げない。実データと誤認しないこと。

```yaml
current-stage: CPF
prefix: GHOST
updated: 2026-07-28
```

現在のステージは **CPF（Customer Problem Fit）**。

<!-- 現在ステージの正本は「最新の stage-transition 意思決定(DEC)の to-stage」。まだ移行の DEC が無い
     プロジェクトはこの current-stage をフォールバックとして使う（`/deciding` がステージ移行時に DEC を作る）。 -->
<!-- prefix はこのプロジェクトのレコードID接頭辞（英数字1トークン・ハイフン不可。例 ACME）。
     `/new-project` が決めた PREFIX を記入する。ツール（records.Project.prefix）はこれを最優先で読むので、
     レコードが1件も無い空プロジェクト期でも PREFIX が確定する（slug と異なる PREFIX も安全）。 -->


このステージの中心の問い・重点仮説タイプ・検証手法・移行基準は [playbooks/cpf.md](../../../playbooks/cpf.md) を参照。

## 移行基準（このプロジェクトの上書き）

デフォルトの移行基準はプレイブックに定義されている。プロジェクト固有に調整する場合はここに上書きを書く（`/deciding` はここを優先して参照する）。

- （上書きなし。playbooks/cpf.md の移行基準を使用）

## ステージ履歴

| 日付 | ステージ | 意思決定 |
|---|---|---|
| 2026-07-28 | CPF（開始） | — |
